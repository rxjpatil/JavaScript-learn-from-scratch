// Declare your name as a string and print its length in JavaScript 
name = "Raj Patil"
console.log(name.length);

// Declare your name as a string and print its first character 
console.log(name[0]);

// Declare your name as a string and print its last character 
console.log(name[8]);

// What is the output of the following  code :

// "raj"+7
console.log("rxj"+7);

// What are lengths of an empty string and a string with a single space
a = "" //0
b = " "  //1

console.log(a.length, b.length);